### Scenario D2 (adversarial) - the other record's endpoint reads a live agent
$ tmux list-panes -t '=fmlive:fm-other3' -F '#{pane_current_command}'
claude
$ ps -o args= -p $(tmux list-panes ... pane_pid)
claude 600
$ bin/fm-teardown.sh mine3 --force
REFUSED: task mine3's recorded worktree /tmp/fm-live-YAoz/trees/.treehouse/project-497a14/3/project is also task other3's recorded worktree, and although the slot's claim names mine3, other3's endpoint reads 'alive', not confidently dead or agent-less.
Returning that pool slot could kill other3's live agent and reset its copy, so nothing was changed - not even with --force.
Reconcile whichever record is wrong (bin/fm-crew-state.sh mine3; bin/fm-crew-state.sh other3), then re-run teardown.

### Scenario E (adversarial) - a leftover claim must not override a secondmate home's record
$ cat $POOL/4/.fm-slot-owner
task=stale4
home=/tmp/fm-live-YAoz/home
$ grep kind state/mate4.meta
kind=secondmate
$ bin/fm-teardown.sh stale4 --force
REFUSED: task stale4's recorded worktree /tmp/fm-live-YAoz/trees/.treehouse/project-497a14/4/project is also task mate4's recorded worktree.
Returning that pool slot would kill mate4's processes and reset its copy, so nothing was changed - not even with --force.
Reconcile whichever record is wrong (bin/fm-crew-state.sh stale4; bin/fm-crew-state.sh mate4), then re-run teardown.
$ treehouse status | sed -n 4p
4     leased       /tmp/fm-live-YAoz/trees/.treehouse/project-497a14/4/project  (held by stale4)
$ ls state/stale4.meta state/mate4.meta
/tmp/fm-live-YAoz/home/state/mate4.meta
/tmp/fm-live-YAoz/home/state/stale4.meta
