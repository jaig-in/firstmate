### Scenario D (adversarial) - claimant tears down while the other record still has a LIVE agent
$ tmux list-panes -t '=fmlive:fm-other3' -F '#{pane_current_command}'
sleep
$ bin/fm-teardown.sh mine3 --force     # mine3 holds the slot claim
REFUSED: task mine3's recorded worktree /tmp/fm-live-YAoz/trees/.treehouse/project-497a14/3/project is also task other3's recorded worktree, and although the slot's claim names mine3, other3's endpoint reads 'ambiguous', not confidently dead or agent-less.
Returning that pool slot could kill other3's live agent and reset its copy, so nothing was changed - not even with --force.
Reconcile whichever record is wrong (bin/fm-crew-state.sh mine3; bin/fm-crew-state.sh other3), then re-run teardown.
exit=

$ treehouse status | sed -n 3p        # slot 3 still leased, never returned
3     leased       /tmp/fm-live-YAoz/trees/.treehouse/project-497a14/3/project  (held by mine3)
$ ls $POOL/3/project/dirty-live-work.txt   # the other agent's uncommitted work survives
/tmp/fm-live-YAoz/trees/.treehouse/project-497a14/3/project/dirty-live-work.txt
$ ls state/mine3.meta state/other3.meta    # neither record removed
/tmp/fm-live-YAoz/home/state/mine3.meta
/tmp/fm-live-YAoz/home/state/other3.meta
$ tmux list-panes -t '=fmlive:fm-other3' -F '#{pane_current_command}'   # the live agent is untouched
sleep
$ cat $POOL/3/.fm-slot-owner
task=mine3
home=/tmp/fm-live-YAoz/home
